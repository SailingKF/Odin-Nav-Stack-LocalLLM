# Bootstrap Prompt For Programmer Thread

From this round onward, use the repository handoff protocol instead of relying on copied chat context.

Rules:
- Before starting work, read `coordination/latest_prompt.md`.
- Treat that file as the current source of truth for scope, constraints, and acceptance.
- Do one coherent bundle only.
- When finished, write `coordination/latest_result.md`.
- The result must include:
  - what you did
  - changed files
  - validation or test results
  - current branch
  - full `git status --short --branch`
  - commit hash
  - whether work is staged and/or committed
  - blockers or limitations
- If blocked, do not broaden scope. Record the blocker clearly in `coordination/latest_result.md`.
- Do not assume the CTO thread can read this chat thread directly. Keep the repository files accurate.

For the current round, read `coordination/latest_prompt.md` and execute it.
